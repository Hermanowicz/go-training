name: Hello, World from Go-training

on: [push]

jobs:
  hello:
    name: Hello, World!
    runs-on: ubuntu-lates
    steps:
      - shell: bash
        run: |
          echo "hello world"

  goodbye:
    name: goodbye
    needs: hello
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          echo "goodbye, world"
